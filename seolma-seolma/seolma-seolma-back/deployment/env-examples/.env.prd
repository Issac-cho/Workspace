# 운영 환경 변수 설정 (AWS Prd VPC)
# EC2 운영 환경에서 사용 (Multi-AZ 이중화)

# Spring Profile
SPRING_PROFILES_ACTIVE=prd

# Database Configuration (RDS Multi-AZ)
DB_HOST=prd-rds-endpoint.xxxxx.ap-northeast-2.rds.amazonaws.com
DB_PORT=3306

# Common DB (General Service: User + Product + Order)
COMMON_DB_NAME=ecommerce_prd_db
COMMON_DB_USERNAME=prd_admin
COMMON_DB_PASSWORD=change_this_strong_password

# Coupon DB (Coupon Service)
COUPON_DB_NAME=coupon_prd_db
COUPON_DB_USERNAME=prd_admin
COUPON_DB_PASSWORD=change_this_strong_password

# JWT Configuration (256비트 이상 강력한 키 사용)
JWT_SECRET=prdSecretKeyForJWTTokenGenerationThatShouldBeAtLeast256BitsLongForProductionEnvironment
JWT_VALIDITY=3600

# CORS Configuration (운영 환경 - 실제 도메인만 허용)
CORS_ALLOWED_ORIGINS=https://www.your-domain.com,https://admin.your-domain.com
CORS_ALLOWED_CREDENTIALS=true

# External Service URLs (Internal NLB 사용 - 권장)
# 방법 1: NLB DNS 엔드포인트 (권장)
COUPON_SERVICE_URL=http://coupon-prd-internal-lb.ap-northeast-2.elb.amazonaws.com:8081

# 방법 2: Route 53 Private Hosted Zone (권장)
# COUPON_SERVICE_URL=http://coupon.prd.internal.ecommerce.local:8081

# 방법 3: 단일 AZ만 사용하는 경우 (비권장)
# COUPON_SERVICE_URL=http://10.0.1.20:8081

# Server Ports
GENERAL_SERVICE_PORT=8080
COUPON_SERVICE_PORT=8081

# File Storage (S3 사용 권장)
FILE_UPLOAD_DIR=/opt/tomcat/uploads
FILE_MAX_SIZE=10485760

# Logging Level
LOGGING_LEVEL_ROOT=WARN
LOGGING_LEVEL_COM_ECOMMERCE=INFO

# Actuator (운영에서는 제한적으로 노출)
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,metrics

# Connection Pool (HikariCP)
DB_POOL_SIZE=20
DB_MAX_LIFETIME=1800000
DB_CONNECTION_TIMEOUT=30000